# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Python caches (none expected here, but safe)
__pycache__/
*.pyc

# SkillHub publish artifact (kept out of git; is a zip of this dir)
*.skill

# Generated audit artifacts (produced by this skill into the agent workspace;
# never part of the skill itself — keep out of git to avoid accidental commits)
clean_code_audit_*.md
clean_code_checklist*.html
